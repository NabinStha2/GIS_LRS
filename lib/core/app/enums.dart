enum OTPFrom { registerOTP, forgotpassword, resendForgotOTP }

enum LoggerType {
  verbose,
  info,
  warning,
  error,
  debug,
  success,
}

enum LoadDataType {
  all,
  explore,
  learning,
  wishlist,
  profile,
}
